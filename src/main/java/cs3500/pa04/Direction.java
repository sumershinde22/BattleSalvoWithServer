package cs3500.pa04;

/**
 * Represents possible direction of a ship
 */
public enum Direction {
  /**
   * direction of ship based on first coord
   */
  VERTICAL,
  /**
   * direction of ship based on first coord
   */
  HORIZONTAL
}
