package cs3500.pa03.controller;

/**
 * The Controller interface represents a controller that controls the flow of the game.
 */
public interface Controller {
  /**
   * Runs the game.
   * This method is responsible for starting and managing the game.
   */
  void run();
}